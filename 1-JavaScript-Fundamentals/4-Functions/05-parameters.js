 /*
    A parameter is a variable in a method definition. Other ways to describe it.. 
    its the variable in the eclaration of the function. and it is found inside the 
    parenthesis. 
    
 */

 // example.. the two parameters are name & type

 function petNameAndBreed(name, type){
     var petDetails = name + ', ' + type;
     console.log(petDetails);
     return petDetails;

 }

 // Key points..  
 // 1) parameters are like variables. 
 // 2) declared in the parenthesis of a function. 
 // 3) the arguments are the valuse that get assigned to the arameters when the function is called. 


 