/*
     ********** for in Loops *********

     the For in Statment is primarily to iterate variables over the enumerable properties. 
     the For in loop executes specific statments. Its best used for looping over objects. 
     to loop over an arry we often need to use the index, to get the value we want. 


*/

// for (var i /* variable section */ in obj /* object section */ ) {
//     console.log(i) //statement 

// }

// write a for loop that captalizes the first letter of studentName 

// var studentName = "paul"; 
// var capSN = '';
// for ( var n in studentName) {
//     if (n == 0) {
//         capSN = studentName[n].toUpperCase();
//     } else {
//         capSN += studentName[n];
//     }
// }
// console.log(capSN);